name: trueforge-safe-rollback

services:
  safe-rollback:
    build:
      context: ./mcp_server
    image: trueforge-safe-rollback-mcp:local
    container_name: ${ARCADEOPS_MCP_CONTAINER_NAME:-trueforge-safe-rollback-mcp-20260825}
    restart: unless-stopped
    environment:
      PORT: "8765"
      STATE_PATH: /data/records.json
      MCP_AUTH_TOKEN: ${TRUEFORGE_MCP_AUTH_TOKEN:?TRUEFORGE_MCP_AUTH_TOKEN must be set}
    volumes:
      - safe-rollback-state:/data
    networks:
      - trueforge
    healthcheck:
      test:
        - CMD
        - node
        - -e
        - fetch('http://127.0.0.1:8765/healthz').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  safe-rollback-state:
    name: ${ARCADEOPS_MCP_STATE_VOLUME:-trueforge-safe-rollback-state}

networks:
  trueforge:
    external: true
    name: trueforge_default
