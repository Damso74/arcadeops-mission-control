A service is failing. Every second matters. The agent knows the rollback. But capability is not permission. Who says yes, and can every step be proved? This is ArcadeOps.

Inside TrueForge, version forty-two is degraded. The mission: investigate, verify, then, only if every gate passes, return checkout API to version forty-one. Authority Contract: one write. Nothing broader.

The agent inspects M C P state: eighteen point four percent errors. TrueForge creates an independent, read-only Verifier. It sees the same incident and plan, but cannot execute it. Two reads. Zero writes. Authority has not moved.

Next, a fail-closed validator runs in Daytona. It calls the M C P tools and checks service, versions, threshold, and state-bound token. Without the exact Sandbox Validation Pass, the workflow stops.

Then, silence. The rollback is ready, but irreversible. TrueForge pauses and shows the action. The participant decides: Allow or Deny. Here, Allow was chosen; interface activation was delegated. The click is not authority. The decision is.

One rollback executes. A fresh inspection finds version forty-one healthy, errors down to zero point seven percent. Replays fail. Concurrent attempts produce one write. Anything out of scope returns Authority Denied.

Now, proof. The Evidence Console creates a portable receipt. Twenty-two checks correlate the reads, Verifier, Daytona, approval, single write, and recovery. Change one required record, and the claim disappears.

ArcadeOps is not blind trust, and not fear of autonomy. It is relief. The agent moves at machine speed. The person keeps the decision. And the evidence stays after both have left the room.
